services:
  mc-server:
    build:
      context: .
      dockerfile: Dockerfile
    image: muj-minecraft:1.20.4
    container_name: mc-server
    ports:
      - "25565:25565"
    environment:
      EULA: "TRUE"
      MEMORY: "2G"
    volumes:
      - ./data:/data
    restart: unless-stopped
